The only place the double-rule appears — add-a-bottle, confirm-remove, recipe detail on desktop.

```jsx
<ModalFrame overlay eyebrow="Add to shelf" title="Rye whiskey"
  footer={<><Button variant="ghost">Cancel</Button><Button>Add</Button></>}>
  …
</ModalFrame>
```

Cards get a single 1px hairline; only frames get 3px + 1px.
