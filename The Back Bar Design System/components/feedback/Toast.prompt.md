Confirmation for shelf changes — always offers Undo.

```jsx
<Toast message="Rye whiskey removed from the shelf." actionLabel="Undo" tone="destructive" onAction={undo} />
```

Sits flush at the bottom of the viewport, full width. Never a floating rounded card.
