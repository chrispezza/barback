Square 44px stepper for bottle counts and parts — arrow keys adjust when the value has focus.

```jsx
<QuantityStepper value={qty} min={0} max={12} unit="btl" onChange={setQty} />
```

Numerals are display Caslon; the unit label uses the caps utility voice. Disabled ends go sage, never hidden.
