Divides the Tonight screen into its sections — the only place the fleuron appears.

```jsx
<MatchHeader label="Drinks" count={14} />
<MatchHeader label="One bottle away" count={6} tone="gap" />
```

Never use the fleuron inline in prose. `tone="gap"` is the only rose usage outside missing-ingredient lines.
