The only button in the system — used for shelf actions, modal footers and empty-state prompts.

```jsx
<Button>Add a bottle</Button>
<Button variant="secondary" size="sm">Filter</Button>
<Button variant="destructive">Remove</Button>
```

Never rounded past 2px, never sans-serif, never sentence case. Destructive is oxblood-outlined and reserved for removal.
