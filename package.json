{
  "name": "barback",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "build:deploy": "tsc -b && VITE_API_URL=/bar VITE_MEILISEARCH_URL=/search vite build"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.4",
    "preact": "^10.29.7",
    "preact-iso": "^2.12.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
